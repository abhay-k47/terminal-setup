[alias]
  co = checkout
  ci = commit
  st = status
  br = branch
  hist = log --pretty=format:'%C(white)%h %C(green)%cs | %C(blue)%s%C(red)%d %C(yellow)[%an]' --graph --date=short
  type = cat-file -t
  dump = cat-file -p

[rerere]
  enabled = true
